<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.himanshudhanwant.uds">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:theme="@style/Theme.AppCompat.Light" -->
        <activity
            android:name=".StartActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Success" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".Home" />
        <activity android:name=".Promo_large" />
        <activity android:name=".UploadItem" />
        <activity android:name=".ItemList" />
        <activity android:name=".UpdateItem" />
        <activity android:name=".Cart" />
        <activity android:name=".Details" />
        <activity
            android:name=".VendorSignUp"
            android:label="@string/title_activity_vendor_sign_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".OrderByMerchant" />
        <activity android:name=".MenuByMerchant" />
        <activity android:name=".SingleItemOrder" />
        <activity android:name=".HomeWithLogin" />
        <activity
            android:name=".Profile"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>